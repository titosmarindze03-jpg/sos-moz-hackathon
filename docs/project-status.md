# Estado do Projecto

## Versão actual

`v1.0-prototipo`

## Estado por área

- frontend: implementado como protótipo;
- backend: não implementado;
- base de dados: não implementada;
- persistência: `localStorage`;
- autenticação: não implementada;
- geolocalização: simulada/conceptual;
- SMS: não implementado;
- IA: não implementada;
- segurança: pendente;
- produção: não pronto.

## Estrutura actual

- frontend: movido para `frontend/`;
- backend: pasta criada em `backend/`, ainda sem implementação;
- docs: mantida em `docs/`;
- regras de IA: mantidas em `.ai/`.

## Resumo executivo

O projecto encontra-se num estado de prototipagem funcional para demonstrar fluxos de resposta humanitária no frontend. Há componentes reais de interface e registo local de dados, mas não existe infra-estrutura real para operação institucional.
