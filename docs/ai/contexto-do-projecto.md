# Contexto do Projecto

O `SOS Moçambique` é actualmente um protótipo frontend-first em `React + Vite`, com persistência simulada em `localStorage`.

## Verdades actuais

- frontend existe;
- backend não existe;
- base de dados real não existe;
- autenticação real não existe;
- GPS real não existe;
- SMS real não existe;
- IA real não existe.

## Regra

Toda alteração futura deve respeitar a diferença entre:

- real;
- simulado;
- planeado.
