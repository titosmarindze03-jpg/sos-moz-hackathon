# API Planeada

## Estado

As rotas abaixo são planeadas, não implementadas.

## Rotas futuras

- `/api/health`
- `/api/people`
- `/api/help-requests`
- `/api/rescue-reports`
- `/api/alerts`
- `/api/safe-zones`

## Regra de verdade

Estas rotas são apenas documentação inicial de planeamento. Não devem ser tratadas como APIs reais até existirem no código.
